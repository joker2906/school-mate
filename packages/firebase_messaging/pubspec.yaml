name: firebase_messaging
description: Local FastAPI-backed firebase_messaging compatibility layer.
version: 0.0.1
environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
